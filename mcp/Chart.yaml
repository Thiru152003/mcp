apiVersion: v2
name: mcp-deployment
description: Helm chart for Model Context Protocol (MCP) deployment
version: 0.1.0
appVersion: "1.0"
dependencies:
  - name: nginx-ingress
    version: 9.1.9
    repository: https://charts.bitnami.com/bitnami
    condition: nginx-ingress.enabled
