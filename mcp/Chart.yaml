apiVersion: v2
name: mcp-deployment
description: Helm chart for Model Context Protocol (MCP) deployment
version: 0.1.0
appVersion: "1.0"
dependencies:
  - name: ingress-nginx
    version: 4.7.1  # Use current stable version
    repository: https://kubernetes.github.io/ingress-nginx
    condition: ingress.enabled
